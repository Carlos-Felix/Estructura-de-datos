#include "menu.h"
#include "arbol_busqueda.h"
#include <iostream>

int main(){
    Menu m;
    m.prensentarMenu();
    //arbol* a;
    //a = m.getArbol();
    //std::cout << "h: " << a->altura(a->getRaiz()) << std::endl;
    //std::cout << "h: " << a->altura(a->encontrarElemento(100)) << std::endl;
    //a->anchuraPrimero(a->getRaiz());
    
    return 0;
}